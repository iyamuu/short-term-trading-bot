"""Risk engine (Phase 4, stub).

position sizing / drawdown guard / exposure guard / circuit breaker. The SQLite
``risk_session`` table is provisioned now; the engine logic lands in later phases.
"""
